@include "arrays.zal" {printhello}
@c {
    #include <stdio.h>
    #include <stdlib.h>
    #include <string.h>
}

func main() {
    //arr := [1, 2, 3, 4, 5]
    arr := [1, 2, 33, 4, 5]
    for i in 0..(len(arr) - 1) {
           print("Value %d \n",arr[i])
    }
    printhello()
}
